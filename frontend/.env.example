# ==============================================================================
# TerraRisk AI - Frontend Environment Configuration Template
# In development, leave empty or set to http://127.0.0.1:5000
# In production with Nginx reverse proxy, leave empty so requests use relative /api
# ==============================================================================

# API Base URL (leave empty for relative /api reverse proxy, or set explicitly)
VITE_API_BASE_URL=
